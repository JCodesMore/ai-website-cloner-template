// VideoSection has been merged into HeroSection so the hero video + the
// "Outdoor Agency" reveal share a single sticky scroll runway, matching the
// original Aurora layout. Kept as an empty no-op for any stray imports.
export function VideoSection() {
  return null;
}
