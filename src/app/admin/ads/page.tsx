import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function AdManagerPage() {
  return (
    <PlaceholderPage
      title="Ad Manager"
      description="Phase 4 — live Meta + Google + TikTok ad data flowing in. Spend, ROAS, CPA per campaign in one grid. One-click pause/resume/scale. Calendar-blocked by client onboarding step 4 (ad account access)."
      icon="📈"
    />
  );
}
