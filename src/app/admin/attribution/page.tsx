import { PlaceholderPage } from "@/components/admin/PlaceholderPage";

export default function AttributionPage() {
  return (
    <PlaceholderPage
      title="Attribution Funnel"
      description="Phase 5 — every booking traced back to its UTM, fbclid, gclid, exact creative variant, and revenue. Cohort by source. ROAS per channel + per creative. The moat — it's built on data you already own (Firestore + n8n workflows)."
      icon="🎯"
    />
  );
}
